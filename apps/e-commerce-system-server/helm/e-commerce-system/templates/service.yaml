apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-commerce-system.fullname" . }}
  labels:
    {{- include "e-commerce-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e-commerce-system.selectorLabels" . | nindent 4 }}
